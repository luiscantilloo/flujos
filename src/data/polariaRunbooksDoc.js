/**
 * Runbooks de operación — Dev Hub (ago 2026).
 */
import { POLARIA_WMS, POLARIA_STATUS_CALLOUT } from './polariaWmsMeta.js'

export function formatPolariaRunbooksMarkdown() {
  return [
    POLARIA_STATUS_CALLOUT,
    '',
    '## Runbooks de operación y deployment',
    '',
    `Procedimientos del producto **${POLARIA_WMS.productName} ${POLARIA_WMS.productVersion}** (${POLARIA_WMS.statusDate}). Hosting típico: web en **Vercel**, API Nest en **Render** (\`0.0.0.0:$PORT\`), Dev Hub en ${POLARIA_WMS.repos.hub.url}.`,
    '',
    '### Runbook 1 — Deploy a producción',
    '',
    '1. Confirmar que `main` (o la rama de release) incluye los PRs aprobados.',
    '2. **API:** en `polaria-wms-api` ejecutar `npm test` y `npm run test:e2e` contra BD de prueba. Luego `npm run build`.',
    '3. **Web:** en `polaria-wms-web` ejecutar `npm test` y `npm run build`.',
    '4. Si el widget cambió: `npm run build:lib` en Widget-react y publicar `mateo-widget.js` (assets de web o CDN).',
    '5. Push / merge que dispare el deploy (Vercel web, Render API).',
    '6. Verificar health: `GET /` en la API y login web con un usuario real.',
    '7. Abrir una bodega: el mapa debe cargar `warehouse_state` (Realtime). Swagger: `GET /api/docs`.',
    '8. Probar un flujo corto (login → selector de bodega → mapa). Si falla, Runbook 2.',
    '',
    'El filesystem de Render es efímero: no guardar archivos locales en el servicio. Migraciones van por `polaria-wms-db` / Supabase, no por copiar SQL al disco del API.',
    '',
    '### Runbook 2 — Rollback',
    '',
    '1. Identificar el último deploy estable (Vercel Deployments / Render Deploys).',
    '2. Web: Promote el deployment anterior a Production.',
    '3. API: redeploy del commit anterior (o rollback del servicio en Render).',
    '4. Si el fallo es de **migración**: no “revertir a ciegas”. Coordinar un SQL inverso o freeze de writes; Postgres no se enrolla con un Promote de frontend.',
    '5. Verificar login + mapa + un endpoint de escritura (p. ej. lock de slot).',
    '6. Avisar al equipo: commit, síntoma y si hay migración a medio aplicar.',
    '',
    '### Runbook 3 — Crear una bodega nueva',
    '',
    '1. Entrar con rol **configurador** o **administrador_cuenta**.',
    '2. En UI: Configuración → Bodegas. Completar nombre, código, tipo (interna/externa), capacidad.',
    '3. El alta **no** se hace insertando en Postgres desde el browser. La web llama `POST /configuracion/bodegas` (bypass RLS en API).',
    '4. Si es **interna**, después: `POST /configuracion/bodegas/:idBodega/bootstrap-layout` (layout de slots).',
    '5. Asignar la bodega a la cuenta/usuarios que corresponda.',
    '6. Cuentas con schema propio: tablas operativas en `emp_*` (migración 062 / `wms_tenant_tables`). Las cuentas legacy siguen en `public`.',
    '',
    '### Runbook 4 — Diagnosticar estado de bodega inconsistente',
    '',
    '1. El estado vivo está en la tabla **`warehouse_state`** (Postgres + Realtime). Una fila por posición.',
    '2. En Supabase: filtrar por `id_bodega`. Revisar JSON de slots / cajas si hay campos `null` inesperados o slots huérfanos.',
    '3. Lock: `POST /inventario/warehouse-state/...` lock / unlock. Un lock stale caduca; el jefe puede force-unlock.',
    '4. Realtime: si el mapa no se actualiza, revisar canal RLS de `warehouse_state` y que el JWT del usuario tenga la bodega en contexto (`X-Id-Bodega`).',
    '5. Logs: Render (API) y Vercel (rutas `/api/*` de Next: n8n, Cloudinary).',
    '6. Script de apoyo en db: `scripts/validate-mapa-pol141.sql` (`estado_slot` vs `warehouse_state`).',
    '',
    '### Runbook 5 — Rotación de secretos',
    '',
    '| Secreto | Dónde | Qué hacer |',
    '| --- | --- | --- |',
    '| Cloudinary | Panel Cloudinary + env web (rutas `/api/evidencia-transporte`) | Nueva API key; redeploy web. El secret no va al cliente. |',
    '| Supabase anon | Dashboard + `NEXT_PUBLIC_SUPABASE_ANON_KEY` / `SUPABASE_ANON_KEY` | Rotar y actualizar web + API. |',
    '| Supabase service role | Solo API | Rotar; **nunca** al browser. Redeploy API. |',
    '| `DATABASE_URL` | API Prisma | Rotar en Render; `prisma generate` ya corre en build. |',
    '| `MATEO_HANDOFF_SECRET` | API | Invalida códigos SSO de 60 s. Redeploy API. |',
    '| `MATEO_WIDGET_JWT_SECRET` | API | Invalida JWT de widget (~300 s). Los chats abiertos pedirán token nuevo. |',
    '| n8n webhooks | Env web + Widget `VITE_N8N_WEBHOOK_URL` | Actualizar URL y rebuild widget si aplica. |',
    '',
    'Anon keys de Supabase son públicas por diseño (RLS las contiene). Si hay brecha: rotar + revisar políticas RLS, no “esconder” la anon key.',
    '',
    '### Runbook 6 — Widget Mateo (token y CDN)',
    '',
    '1. Host en web: `MateoWidgetHost` pide `POST /auth/mateo/widget-token` (JWT ~300 s) para n8n. Ese token **no** es la sesión de 12 h: se renueva solo.',
    '2. Historial usa el **Bearer de sesión WMS** en `/mateo/conversaciones`, no el JWT de n8n.',
    '3. Logout o tope de 12 h en Polaria: reset del token Mateo y cierre del widget (no dejar el chat abierto con WMS cerrado).',
    '4. Header `X-Auth-Client: wms` o `mateo` en prelogin.',
    '5. Schema: `mateo_support` + vistas `public.widget_*` (migración 064).',
    '6. Tras cambiar el widget: `npm run build:lib` y copiar el IIFE a `polaria-wms-web` (`/assets/mateo-widget.js` o `NEXT_PUBLIC_MATEO_WIDGET_SCRIPT_URL`).',
    '',
    '### Runbook 7 — Documentación del Dev Hub',
    '',
    '- Checklist, onboarding, runbooks, API, seguridad, testing y Mateo se generan desde `src/data/*.js`. Editá esos archivos.',
    '- **No** ejecutes `npm run docs:bodega-v20` ni `docs:all` para “actualizar” el hub: pisan `bodega_de_frio_documentacion_v2_0.md` desde el `.txt` viejo.',
    '- `npm run docs:sync-schema` sí: sincroniza entidades Prisma hacia el hub.',
  ].join('\n')
}
