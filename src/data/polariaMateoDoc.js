/**
 * Mateo Support — referencia Dev Hub (ago 2026).
 * Fuentes: Widget-react/docs, polaria-wms-api auth + mateo-widget, migraciones 051–057 / 064.
 */
import { POLARIA_WMS, POLARIA_STATUS_CALLOUT } from './polariaWmsMeta.js'

export function formatPolariaMateoMarkdown() {
  return [
    POLARIA_STATUS_CALLOUT,
    '',
    '## Mateo Support — widget de chat',
    '',
    'Asistente de IA embebido en **polaria-wms-web** (solo sesión autenticada). Repo UI: [' +
      POLARIA_WMS.repos.widget.name +
      '](' +
      POLARIA_WMS.repos.widget.url +
      '). Persistencia: Nest `mateo-widget` + schema Postgres `mateo_support`.',
    '',
    '### Repos y piezas',
    '',
    '| Pieza | Responsabilidad |',
    '| --- | --- |',
    `| Widget-react | Shadow DOM, IIFE \`mateo-widget.js\`, n8n, Cloudinary, i18n es/en |`,
    `| polaria-wms-web | Host \`MateoWidgetHost\`, tokenFetcher, conversationApiBase |`,
    `| polaria-wms-api | JWT widget, SSO, CRUD \`/mateo/conversaciones\` |`,
    `| polaria-wms-db | Tablas en \`mateo_support\` + vistas \`public.widget_*\` (064) |`,
    '| n8n (externo) | Workflow conversacional Mateo |',
    '| Cloudinary | Imágenes adjuntas (upload unsigned) |',
    '',
    '### Dos tokens (no intercambiables)',
    '',
    '| Uso | Credencial | Nest | Cliente web hoy |',
    '| --- | --- | --- | --- |',
    '| Mensaje a n8n | JWT widget ~300 s | `POST /auth/mateo/widget-token` | se refresca mientras hay sesión WMS |',
    '| Historial | Bearer sesión WMS | `/mateo/conversaciones` | hasta 12 h o logout |',
    '| SSO app externa | Código 60 s | `POST /auth/mateo-handoff` → `POST /auth/mateo-exchange` | igual |',
    '',
    'Header de cliente: `x-auth-client: wms | mateo` (el front envía `X-Auth-Client`; HTTP es case-insensitive). Prelogin: `POST /auth/prelogin`.',
    '',
    '### CRUD historial (Bearer WMS, no JWT n8n)',
    '',
    '| Método | Ruta |',
    '| --- | --- |',
    '| GET | `/mateo/conversaciones` |',
    '| GET | `/mateo/conversaciones/:id` |',
    '| POST | `/mateo/conversaciones` |',
    '| POST | `/mateo/conversaciones/:id/mensajes` |',
    '| DELETE | `/mateo/conversaciones/:id` |',
    '',
    '`POST .../mensajes` es **idempotente** ante reintentos (mismo rol/tipo/contenido/esError/createdAt).',
    '',
    '### Modelo de datos',
    '',
    '- Schema físico **`mateo_support`**: `widget_conversacion`, `widget_mensaje` (migraciones 051, 055, 057).',
    '- Prisma: `WidgetConversacion`, `WidgetMensaje` con `@@map("widget_conversacion")` **sin** `@@schema`. Por eso 064 crea vistas `public.widget_conversacion` / `public.widget_mensaje`.',
    '- RLS: el usuario solo ve sus hilos; `codigo_cuenta` debe coincidir (anti-spoof POL-138).',
    '- En embed, la API es fuente de verdad; localStorage es cache. Ids locales `conv_*` se remapean a UUID al persistir.',
    '',
    '### Arquitectura UI (Widget-react)',
    '',
    '1. **Shadow DOM** — el preflight de Tailwind no rompe el CSS del WMS (ADR-0001).',
    '2. **SweetAlert2** no puede vivir en el shadow root; su CSS va a `document.head` con clases `mateo-swal-*`.',
    '3. Capas: `components/` (presentación) → `hooks/` (orquestación) → `lib/` (fetch, storage, webhook). `lib/` no importa React.',
    '4. Host debe llamar `MateoWidget.configureTokenFetcher` **antes** de `mount`.',
    '5. **Sesión 12 h:** el host de Polaria corta el tokenFetcher y desmonta el widget al logout o al vencer `SESSION_MAX_AGE_MS`.',
    '6. **Enlaces:** `parseRichContent` marca URLs y nombres `.pdf` como `link` (subrayado, nueva pestaña; PDF con `download` si hay URL).',
    '7. **n8n body:** plano `{ message_text, message_type, conversation_id (UUID), phone_number, …claims }`. No mandar ids temporales `conv_*`. `conversation_id` no viaja en el JWT del widget.',
    '',
    'Docs de origen en el widget: `docs/ARQUITECTURA.md`, `docs/EMBED-POLARIA.md`, `docs/SEGURIDAD.md`, ADRs 0001–0006.',
    '',
    '### Variables',
    '',
    '| Repo | Variable |',
    '| --- | --- |',
    '| API | `MATEO_HANDOFF_SECRET`, `MATEO_WIDGET_JWT_SECRET`, `MATEO_WIDGET_JWT_ISSUER`, `MATEO_ALLOWED_ORIGINS` |',
    '| Web | `NEXT_PUBLIC_MATEO_WIDGET_SCRIPT_URL` (p. ej. `/assets/mateo-widget.js`) |',
    '| Widget | `VITE_N8N_WEBHOOK_URL`, `VITE_CLOUDINARY_CLOUD_NAME`, `VITE_CLOUDINARY_UPLOAD_PRESET` |',
    '',
    '### Pendiente',
    '',
    '- 🟡 CDN estable de `mateo-widget.js` en producción.',
    '- 🔵 Rate limit / abuso del webhook n8n.',
    '',
    'Manual de usuario: `/manual-usuario/proceso-mateo`. Mapa de producto: documentación *Polaria WMS — mapa actual*.',
  ].join('\n')
}
